package auto;

import java.awt.AWTException;
import java.awt.MouseInfo;
import java.awt.Point;
import java.awt.Rectangle;
import java.awt.Robot;
import java.awt.event.InputEvent;
import java.awt.image.RenderedImage;
import java.io.File;
import java.io.IOException;

import javax.imageio.ImageIO;

public class Download 
{
	public static void getPointerLocation()
	{
		Point p = MouseInfo.getPointerInfo().getLocation();
		System.out.println(p.x + " " + p.y);
	}
	
	public static void main(String[] args) throws Exception 
	{
		Robot robot = new Robot();
		
//		while(true)
//		{
//			getPointerLocation();
//			robot.delay(1000);
//		}
		
		for(int i = 0 ; i < 10; i++)
		{
			RenderedImage bufferedImage = robot.createScreenCapture(new Rectangle(220, 100, 900, 400));
			String name = "E://image" + i + ".jpg";
			File outputfile = new File(name);
			ImageIO.write(bufferedImage, "jpg", outputfile);
			robot.delay(1000);
		}
		
		
		int times = 5;
		robot.delay(2000);
		
		for(int i = 0; i < times; i++)
		{
			robot.mouseMove(385, 600);
			robot.mousePress(InputEvent.BUTTON1_MASK);
			robot.mouseRelease(InputEvent.BUTTON1_MASK);
			
			robot.delay(3000);
			robot.mouseMove(370, 625);
			robot.mousePress(InputEvent.BUTTON1_MASK);
			robot.mouseRelease(InputEvent.BUTTON1_MASK);
			robot.delay(2500);
		
			robot.mouseMove(480, 450);
			robot.mousePress(InputEvent.BUTTON1_MASK);
			robot.mouseRelease(InputEvent.BUTTON1_MASK);
			robot.delay(3000);
			
			robot.mouseMove(240, 495);
			robot.mousePress(InputEvent.BUTTON1_MASK);
			robot.mouseRelease(InputEvent.BUTTON1_MASK);
			robot.delay(3000);
		}
	}
}
